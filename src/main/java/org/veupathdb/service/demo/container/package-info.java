/**
 * The container package contains a default base toolset for constructing a
 * service.
 *
 * The middleware subpackage contains filters, providers, and handlers to
 * be plugged into the running jersey server.
 *
 * The utils subpackage contains miscellaneous helpers, utilities, and
 * singletons.
 */
package org.veupathdb.service.demo.container;
