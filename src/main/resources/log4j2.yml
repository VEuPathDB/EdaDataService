Configuration:
  name: Default
  status: error
  Appenders:
    Console:
      name: Console_Appender
      target: SYSTEM_OUT
      PatternLayout:
        pattern: "%highlight{%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level][%-22X{context-id}]: %msg%n}"
  Loggers:
    Root:
      level: trace
      AppenderRef:
        - ref: Console_Appender
