package org.veupathdb.service.eda.ds.plugin.standalonemap.aggregator;

import com.google.common.collect.Sets;
import org.junit.jupiter.api.Test;

import java.util.stream.Collectors;
import java.util.stream.IntStream;

import static org.junit.jupiter.api.Assertions.*;

public class CategoricalProportionAggregatorTest {

  @Test
  public void test() {
//    final String[] record = new String[] {"5"};
//    CategoricalProportionAggregator agg = new CategoricalProportionAggregator(
//        Sets.newHashSet(IntStream.range(0, 100).mapToObj(Integer::toString).collect(Collectors.toSet())),
//        IntStream.range(0, 1000).mapToObj(Integer::toString).collect(Collectors.toSet()),
//        0);
//    for (int x = 0; x < 10; x++) {
//      for (int i = 0; i < 2_000_000_000; i++) {
//        agg.addValue(record);
//      }
//    }
  }

}